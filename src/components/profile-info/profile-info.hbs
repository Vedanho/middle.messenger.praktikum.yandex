<div class="profile-info">
  {{#each profileInfo}}
    <div class="profile-info__item">
      <span class="profile-info__name">{{keyName}}</span>
      {{#if ../isChangeProfile}}
        {{> Input type="text"}}
      {{else}}
        <span class="profile-info__value">{{value}}</span>
      {{/if}}
    </div>
  {{/each}}
</div>